import { render } from "@testing-library/react";

describe("", () => {
  it("", () => {
    const Hi = () => {
      return <div>Hi</div>;
    };
    render(<Hi />);
    expect(Hi).toBeDefined();
  });
});
