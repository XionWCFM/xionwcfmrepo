import { ASSET_XION_THUMBNAIL_FALLBACK_336_115 } from "~/shared/assets";

export const getThumbnail = (param: string) => {
  switch (param) {
    default:
      return ASSET_XION_THUMBNAIL_FALLBACK_336_115;
  }
};
